{
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check app.js && node --check admin.js && node --check login.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1"
  }
}
